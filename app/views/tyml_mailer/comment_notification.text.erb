New comment from <%= @comment.user.name %>

<%= @comment.comment_text %>

Re: <%= link_to "#{@comment.tyml.url}", @comment.tyml.url %>

<% if @comment.tyml.receiver.confirmed %>
 <%= link_to "Reply from your TYML dashboard", root_url %>
<% else %>
 <%= link_to "Reply by signing up for TYML", root_url %>
<% end %>

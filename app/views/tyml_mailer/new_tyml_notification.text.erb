Hey!

<%= @tyml.sender.name %> Though You Might Like...

      <%= @tyml.url %>

      Because: <%= @tyml.statement %>

      <%= @tyml.sender.name %>

      <% if @tyml.receiver.confirmed %>
        <%= link_to "Reply from your TYML dashboard", root_url %>
      <% else %>
        <%= link_to "Reply by signing up for TYML", root_url %>
      <% end %>


TYML is the best way to share content with people you care about.
